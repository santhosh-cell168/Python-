version: 0.0
os: linux
files:
  -source: /
   destination: /home/ubunutu/python-app
hooks:
  AfterInstall:
    -location: scripts/stop_server.sh
      timeout: 300
      runas: root
  ApplicationStart:
     -location: scripts/start_server.sh
      timeout: 300
      runas:root
